services:
  - type: web
    name: omixbooks
    runtime: node
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: omixbooks-db
          property: connectionString
      - key: NODE_ENV
        value: production
